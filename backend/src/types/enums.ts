export enum StrategyStatus {
  RUNNING = "running",
  STOPPED = "stopped"
}

export enum RollOverStatus {
  ENABLED = "enabled",
  DISABLED = "disabled"
}

export enum SymbolType {
  FUTURE = "future",
  OPTIONS = "options"
}
